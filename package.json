{
  "name": "vacabweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12 <25"
  },
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "build": "npm run dictionaries:build --prefix backend && npm run build --prefix frontend && npm run build --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "start": "node scripts/start-prod.mjs",
    "test:e2e:community-account": "node scripts/e2e-community-account.mjs",
    "test:e2e:collaboration": "node scripts/e2e-collaboration.mjs"
  },
  "devDependencies": {
    "playwright-core": "1.62.0"
  }
}
